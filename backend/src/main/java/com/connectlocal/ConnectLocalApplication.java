package com.connectlocal;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the ConnectLocal Hyperlocal Service Marketplace backend.
 */
@SpringBootApplication
public class ConnectLocalApplication {

    public static void main(String[] args) {
        SpringApplication.run(ConnectLocalApplication.class, args);
    }
}
